---
- name: Validate Ghostty configuration
  ansible.builtin.command:
    argv:
      - "{{ ghostty_executable }}"
      - +show-config
      - --changes-only
  environment:
    XDG_CONFIG_HOME: "{{ dotfiles_xdg_config_home }}"
  changed_when: false
